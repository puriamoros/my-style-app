<?xml version="1.0" encoding="utf-8" ?>
<views:CustomContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyStyleApp.Views.EstablishmentServicesView"
             xmlns:views="clr-namespace:MyStyleApp.Views"
             Padding="{StaticResource PagePadding}">

  <StackLayout>
    <ListView x:Name="GroupedView" ItemsSource="{Binding GroupedTextDataList}"
             GroupDisplayBinding="{Binding Key}"
             GroupShortNameBinding="{Binding ShortKey}"
             IsGroupingEnabled="true">

      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Padding="10,5,0,5">
              <Label Text="{Binding Title}" FontSize="24"/>
              <Label Text="{Binding Subtitle}" FontSize="16"/>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
  
</views:CustomContentPage>